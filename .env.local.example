# Copia questo file in .env.local (già ignorato da git) e riempi con i valori
# reali del tuo progetto Supabase: Project Settings -> API.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# NON serve qui la service role key: quella va SOLO nelle env var del progetto
# Vercel (mai in un file .env.local, mai con prefisso VITE_), perché la usa
# solo la funzione serverless in api/invite-athlete.js.
